# SpringBrand-specific Hermes config for Railway deployment.
#
# This file is COMMITTED to the springbrand-customizations branch so the
# config is reproducible / auditable. Railway entrypoint (or Dockerfile
# CMD) is responsible for placing this at $HERMES_HOME/config.yaml before
# starting `hermes gateway run`. Local dev uses
# /tmp/springbrand-hermes-demo/config.yaml generated by sp-a2a-agents-backend
# scripts/start-hermes-local.sh — both should keep platform_toolsets in
# sync.
#
# Wire:
#   demo agent-backend → POST /v1/runs (this Hermes daemon, via API server
#   platform) → springbrand_supost tool → supost-adapter → supost.com

model:
  provider: openrouter
  default: openai/gpt-4.1-mini
  base_url: https://openrouter.ai/api/v1

# API server platform exposes ONLY the SUpost concierge tool to LLM. We
# intentionally drop hermes-api-server's default toolset (web/terminal/
# browser/file write) — buyer concierge runs in production where shell
# access is unwanted and the LLM should focus on the supost decision flow.
# To extend with more business tools (medusa, etc.) add them to this list
# and import the matching tools/*_tool.py module.
platform_toolsets:
  api_server:
    - springbrand_supost
